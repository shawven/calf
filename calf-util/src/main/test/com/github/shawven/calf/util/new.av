let r = range(-5, 5);
let list = seq.list(seq.map("a", 1), seq.map("a", 2));


fn PP(f, &args) {
  return fn(&remaining) {
    return f(*args, *remaining);
  };
}

fn FOR(l, exp) {
  p(__env__)
}

FOR(range(-5, 5))(%{x})

